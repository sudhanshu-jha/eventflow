apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker-notifications
  namespace: eventflow
  labels:
    app: celery-worker
    queue: notifications
    app.kubernetes.io/name: celery-worker-notifications
    app.kubernetes.io/component: worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: celery-worker
      queue: notifications
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: celery-worker
        queue: notifications
        app.kubernetes.io/name: celery-worker-notifications
        app.kubernetes.io/component: worker
    spec:
      containers:
        - name: celery-worker
          image: eventflow-backend:latest
          imagePullPolicy: Always
          command:
            - celery
            - -A
            - analytics.tasks.celery_app
            - worker
            - --loglevel=info
            - -Q
            - notifications
            - -c
            - "4"
            - --without-heartbeat
            - --without-gossip
            - --without-mingle
          envFrom:
            - configMapRef:
                name: eventflow-config
            - secretRef:
                name: eventflow-secrets
          env:
            - name: OTEL_SERVICE_NAME
              value: "eventflow-celery-notifications"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.namespace=eventflow,deployment.environment=production,celery.queue=notifications"
          resources:
            requests:
              cpu: 125m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command:
                - celery
                - -A
                - analytics.tasks.celery_app
                - inspect
                - ping
                - -d
                - celery@$HOSTNAME
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
      terminationGracePeriodSeconds: 300
