apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: eventflow
  labels:
    app: backend
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 6543
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: backend
